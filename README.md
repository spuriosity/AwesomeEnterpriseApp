# AwesomeEnterpriseApp

University project (2011–2012). ASP.NET MVC 4 app for finding film locations — parses XML location data, does distance calculations, serves results through a web UI.

Includes unit tests, a data model doc, and a separate XML parser prototype. The name was assigned, not chosen.

C# / .NET Framework 4.0 / MSTest
